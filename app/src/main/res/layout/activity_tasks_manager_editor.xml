<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.nc.nc_android.TasksManagerEditorActivity">

    <ListView
        android:id="@+id/lvGameTasks"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="255dp"
        android:choiceMode="singleChoice"
        android:listSelector="@drawable/bg_key" />

    <Button
        android:id="@+id/btnEditSelectedTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="183dp"
        android:layout_marginStart="26dp"
        android:text="Edit" />

    <Button
        android:id="@+id/btnNewTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@+id/btnEditSelectedTask"
        android:layout_marginEnd="39dp"
        android:layout_marginTop="84dp"
        android:text="New" />

    <TextView
        android:id="@+id/tvTaskSelection"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/btnEditSelectedTask"
        android:layout_alignTop="@+id/btnEditSelectedTask"
        android:layout_marginStart="19dp"
        android:layout_toEndOf="@+id/btnEditSelectedTask"
        android:text="Selected item" />

    <TextView
        android:id="@+id/tvDebugManager"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/btnEditSelectedTask"
        android:layout_below="@+id/btnNewTask"
        android:layout_marginStart="59dp"
        android:text="Debug" />

    <Button
        android:id="@+id/btnReorder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/btnNewTask"
        android:layout_below="@+id/btnNewTask"
        android:text="Reorder" />
</RelativeLayout>